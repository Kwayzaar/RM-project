import React, {useState} from 'react'

function LocationButtons({ locations }) {

  return (
    <ul>
      <li>
        <button>
          
        </button>
      </li>
    </ul>
  )
}

export default LocationButtons