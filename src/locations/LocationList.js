import React, {useState} from 'react'

function LocationList({ locations }) {

  return (
    <ul>
      {/* <h2 className="flex items-center text-center h-full" alt={location.name}>{ location.name }</h2> */}
    </ul>
  )
}

export default LocationList